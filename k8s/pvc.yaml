# k8s/pvc.yaml – Persistent storage for MySQL database
# Apply: kubectl apply -f k8s/pvc.yaml

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
